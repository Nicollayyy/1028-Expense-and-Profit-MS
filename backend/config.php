<?php
// config.php - local configuration for the application.
//
// This file is intended to hold non-secret configuration values such as
// folder IDs or local file paths. For secrets (service account files, keys)
// prefer storing them outside version control and referencing them via
// absolute paths or environment variables.
//
// Usage: return an associative array with keys used by the app, for example:
// return [
//     'DRIVE_FOLDER_ID' => 'your-drive-folder-id',
//     'DRIVE_CREDENTIALS_PATH' => __DIR__ . '/credentials.json',
// ];

return [];
